---
phase: 01-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [
  "lib/routes/app_router.dart.backup",
  "lib/models/call.dart",
  "lib/models/call_participant.dart",
  "lib/models/story.dart",
  "lib/models/story_model.dart",
  "lib/models/notification.dart",
  "lib/features/auth/presentation/screens/registration_screen.dart"
]
autonomous: false
requirements: [CLEANUP-01, CLEANUP-02]

must_haves:
  truths:
    - "Unused legacy .dart files identified in RESEARCH.md are removed."
    - "The project analysis (flutter analyze) passes successfully."
    - "The application launches and functions correctly (smoke test)."
  artifacts:
    - path: "lib/models/call.dart"
      provides: "Legacy call model"
      min_lines: 0 # Should not exist
    - path: "lib/models/notification.dart"
      provides: "Legacy notification model"
      min_lines: 0 # Should not exist
  key_links:
    - from: "lib/routes/app_router.dart"
      to: "removed files"
      via: "import"
      pattern: "import.*(notification\\.dart|call\\.dart|story_model\\.dart|story\\.dart|call_participant\\.dart)"
---

<objective>
Remove unused legacy .dart files and perform final validation to ensure build integrity and application stability.

Purpose: Finalize the codebase cleanup by deleting verified unused files.
Output: A cleaner codebase with no legacy models and a passing analysis.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-1/RESEARCH.md
@.planning/phases/phase-1/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cleanup Wave - Delete Unused .dart Files</name>
  <files>
    lib/routes/app_router.dart.backup,
    lib/models/call.dart,
    lib/models/call_participant.dart,
    lib/models/story.dart,
    lib/models/story_model.dart,
    lib/models/notification.dart,
    lib/features/auth/presentation/screens/registration_screen.dart
  </files>
  <action>
    1. Verify that all identified legacy files were confirmed as unused in Plan 01-01.
    2. Delete the following files from the filesystem:
       - lib/routes/app_router.dart.backup
       - lib/models/call.dart
       - lib/models/call_participant.dart
       - lib/models/story.dart
       - lib/models/story_model.dart
       - lib/models/notification.dart
       - lib/features/auth/presentation/screens/registration_screen.dart
    3. Ensure NO .sql, asset, or script files are modified or deleted.
  </action>
  <verify>
    <automated>
      # Verify files are gone
      for file in lib/routes/app_router.dart.backup lib/models/call.dart lib/models/call_participant.dart lib/models/story.dart lib/models/story_model.dart lib/models/notification.dart lib/features/auth/presentation/screens/registration_screen.dart; do
        if [ -f "$file" ]; then echo "FAIL: $file still exists"; exit 1; fi
      done
      echo "PASS: All legacy .dart files removed."
    </automated>
  </verify>
  <done>Legacy .dart files deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Final Verification - Flutter Analyze</name>
  <files>N/A</files>
  <action>
    1. Run `flutter analyze` across the entire project.
    2. Confirm that there are zero errors (warnings/hints can be reviewed but should not block completion unless related to the cleanup).
    3. If errors occur, restore files from git or fix broken imports as necessary.
  </action>
  <verify>
    <automated>
      flutter analyze
    </automated>
  </verify>
  <done>Project integrity verified with 'flutter analyze'.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Smoke Test Checkpoint</name>
  <files>N/A</files>
  <action>
    <what-built>A cleaner project tree after successful legacy file removal.</what-built>
    <how-to-verify>
      1. Launch the application (flutter run) on a physical device or emulator.
      2. Perform a basic walkthrough:
         - Login/Registration (ensure new entities work)
         - Feed navigation
         - Initiate/Receive a call (if possible)
         - View notifications
      3. Confirm that the app remains stable and no runtime crashes occur due to missing assets or models.
    </how-to-verify>
  </action>
  <verify>Type "approved" in the chat if the app remains stable.</verify>
  <done>Human confirmation of application stability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File Deletion -> Runtime Stability | Risk of missing components at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Availability | Application | mitigate | Smoke test checkpoint covers runtime regression. |
</threat_model>

<verification>
- [ ] Legacy .dart files are removed.
- [ ] 'flutter analyze' passes.
- [ ] Smoke test confirmed by human.
</verification>

<success_criteria>
- Unused legacy .dart files are gone.
- Build integrity is preserved.
- Application remains stable.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-1/01-02-SUMMARY.md`
</output>
