---
phase: 01-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "lib/services/notification_service.dart",
  "lib/routes/app_router.dart",
  "lib/services/call_service.dart",
  "lib/services/spotify_service.dart",
  "lib/widgets/stories/music_picker_sheet.dart",
  "lib/features/stories/presentation/screens/create_story_screen.dart",
  "lib/features/calls/presentation/screens/active_call_screen.dart",
  "lib/features/calls/presentation/screens/incoming_call_overlay.dart"
]
autonomous: true
requirements: [CLEANUP-01, CLEANUP-02]

must_haves:
  truths:
    - "Legacy model imports are replaced with new feature-driven entities."
    - "No imports or references to legacy .dart files remain in the project."
  artifacts:
    - path: "lib/services/notification_service.dart"
      provides: "Uses AppNotification entity"
      contains: "import 'package:oasis/features/notifications/domain/models/notification_entity.dart';"
    - path: "lib/services/call_service.dart"
      provides: "Uses CallEntity"
      contains: "import 'package:oasis/features/calling/domain/models/call_entity.dart';"
  key_links:
    - from: "lib/services/notification_service.dart"
      to: "lib/features/notifications/domain/models/notification_entity.dart"
      via: "import"
      pattern: "import.*notification_entity\\.dart"
---

<objective>
Migrate active dependencies on legacy models to new entity classes and verify that no references to legacy files remain.

Purpose: Decouple the codebase from deprecated models to allow safe deletion.
Output: Migrated service and UI files with zero legacy references.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-1/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Git Snapshot & Migration Wave</name>
  <files>
    lib/services/notification_service.dart,
    lib/routes/app_router.dart,
    lib/services/call_service.dart,
    lib/services/spotify_service.dart,
    lib/widgets/stories/music_picker_sheet.dart,
    lib/features/stories/presentation/screens/create_story_screen.dart,
    lib/features/calls/presentation/screens/active_call_screen.dart,
    lib/features/calls/presentation/screens/incoming_call_overlay.dart
  </files>
  <action>
    1. Create a snapshot of the current state: `git checkout -b cleanup/migration-phase-1`.
    2. Per RESEARCH.md, update imports and class references in the specified files to use:
       - `AppNotification` from `lib/features/notifications/domain/models/notification_entity.dart`
       - `CallEntity` from `lib/features/calling/domain/models/call_entity.dart`
       - `StoryEntity` from `lib/features/stories/domain/models/story_entity.dart`
    3. Ensure any helper methods or property accesses are updated to match the new entity structures.
  </action>
  <verify>
    <automated>
      # Verify imports in key files
      grep "notification_entity.dart" lib/services/notification_service.dart
      grep "call_entity.dart" lib/services/call_service.dart
      grep "story_entity.dart" lib/features/stories/presentation/screens/create_story_screen.dart
    </automated>
  </verify>
  <done>Active dependencies migrated to new entities.</done>
</task>

<task type="auto">
  <name>Task 2: Verification Wave - Audit Legacy References</name>
  <files>N/A</files>
  <action>
    1. Perform a comprehensive recursive search (grep) across 'lib/' for all legacy filenames:
       - notification.dart
       - call.dart
       - story_model.dart
       - story.dart
       - call_participant.dart
       - app_router.dart.backup
       - registration_screen.dart
    2. Confirm that there are ZERO results (excluding the legacy files themselves).
    3. If any results are found, report them as blockers for deletion.
  </action>
  <verify>
    <automated>
      # Search for references, ignoring the files themselves
      grep_results=$(grep -r "notification.dart\|call.dart\|story_model.dart\|story.dart\|call_participant.dart\|app_router.dart.backup\|registration_screen.dart" lib/ | grep -v "lib/models/\|lib/features/auth/presentation/screens/registration_screen.dart\|lib/routes/app_router.dart.backup")
      if [ -z "$grep_results" ]; then
        echo "PASS: No references found."
      else
        echo "FAIL: References found: $grep_results"
        exit 1
      fi
    </automated>
  </verify>
  <done>Zero references confirmed across the codebase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration -> Existing Logic | Risk of breaking logic when changing models. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Integrity | lib/services | mitigate | Use 'flutter analyze' in Wave 3 to catch type mismatches. |
| T-01-01-02 | Availability | Runtime | mitigate | Smoke test in Wave 3 to ensure app stability. |
</threat_model>

<verification>
- [ ] No references to legacy files in 'lib/'.
- [ ] Imports in services are updated.
</verification>

<success_criteria>
- The codebase is ready for legacy file deletion without breaking existing features.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-1/01-01-SUMMARY.md`
</output>
