---
phase: 01-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "lib/routes/app_router.dart.backup",
  "lib/models/call.dart",
  "lib/models/call_participant.dart",
  "lib/models/story.dart",
  "lib/models/story_model.dart",
  "lib/models/notification.dart",
  "lib/features/auth/presentation/screens/registration_screen.dart"
]
autonomous: false
requirements: [CLEANUP-01, CLEANUP-02]

must_haves:
  truths:
    - "Identified unused .dart files are removed from the filesystem."
    - "No broken imports exist across the remaining codebase."
    - "Project analysis (flutter analyze) passes with zero errors."
    - "The app can still be built and launched (smoke test)."
  artifacts:
    - path: "lib/routes/app_router.dart.backup"
      provides: "Old router backup"
      min_lines: 0 # Should not exist
    - path: "lib/models/call.dart"
      provides: "Legacy call model"
      min_lines: 0 # Should not exist
  key_links:
    - from: "lib/routes/app_router.dart"
      to: "removed files"
      via: "import"
      pattern: "import.*(app_router\\.dart\\.backup|call\\.dart|call_participant\\.dart|story\\.dart|story_model\\.dart|notification\\.dart|registration_screen\\.dart)"
---

<objective>
Remove unused .dart files identified in RESEARCH.md to clean up technical debt and focus on the new feature-driven architecture. 

Purpose: Reduce codebase noise and prevent accidental usage of legacy models.
Output: A cleaner codebase with verified dependency integrity.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-1/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and Remove Unused .dart Files</name>
  <files>
    lib/routes/app_router.dart.backup,
    lib/models/call.dart,
    lib/models/call_participant.dart,
    lib/models/story.dart,
    lib/models/story_model.dart,
    lib/models/notification.dart,
    lib/features/auth/presentation/screens/registration_screen.dart
  </files>
  <action>
    1. Perform a final recursive search (grep) across the 'lib/' directory to ensure NO files import or reference the candidate files:
       - app_router.dart.backup
       - models/call.dart
       - models/call_participant.dart
       - models/story.dart
       - models/story_model.dart
       - models/notification.dart
       - registration_screen.dart
    2. If any references are found, STOP and report them.
    3. If no references are found, delete the identified files.
    4. Run 'flutter analyze' to verify that the removals did not break the project.
  </action>
  <verify>
    <automated>
      # 1. Verify files are gone
      for file in lib/routes/app_router.dart.backup lib/models/call.dart lib/models/call_participant.dart lib/models/story.dart lib/models/story_model.dart lib/models/notification.dart lib/features/auth/presentation/screens/registration_screen.dart; do
        if [ -f "$file" ]; then echo "FAIL: $file still exists"; exit 1; fi
      done
      # 2. Run analysis
      flutter analyze
    </automated>
  </verify>
  <done>Candidates removed and project analysis passes.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Final Confirmation and Smoke Test</name>
  <files>N/A</files>
  <action>
    <what-built>A codebase free of identified unused legacy .dart files.</what-built>
    <how-to-verify>
      1. Verify that the following files are no longer in the project tree:
         - lib/routes/app_router.dart.backup
         - lib/models/call.dart, call_participant.dart, story.dart, story_model.dart, notification.dart
         - lib/features/auth/presentation/screens/registration_screen.dart
      2. Attempt to build the app (e.g., 'flutter build apk --debug' or 'flutter run' if a device is connected).
      3. Navigate through basic flows (Login, Feed) to ensure no runtime errors related to missing models or screens.
    </how-to-verify>
  </action>
  <verify>Type "approved" in the chat if the app remains stable.</verify>
  <done>Human confirmation that the app remains stable after cleanup.</done>
</task>

</tasks>

<safety>
## Safety Considerations
- **Git Snapshots:** Ensure a clean git state before starting. It is recommended to create a temporary branch: `git checkout -b cleanup/phase-1-dart`.
- **Exclusions:** This phase explicitly avoids .sql, assets, and script files.
- **Rollback:** If 'flutter analyze' fails, files can be restored via `git checkout -- <file_path>` or `git stash pop`.
</safety>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toolchain -> Codebase | flutter analyze / dart compile are trusted to detect broken references. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | lib/ | mitigate | Automated verification of no imports BEFORE deletion. |
| T-01-02 | Availability | build | mitigate | 'flutter analyze' gate after deletion to prevent broken builds. |
</threat_model>

<verification>
- [ ] No candidate files exist in the filesystem.
- [ ] No files in lib/ have broken imports.
- [ ] 'flutter analyze' returns no errors.
</verification>

<success_criteria>
- Project directory is free of identified legacy .dart files.
- Build integrity is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-1/01-01-SUMMARY.md`
</output>
