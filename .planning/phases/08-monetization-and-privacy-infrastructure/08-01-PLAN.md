---
phase: 08-monetization-and-privacy-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/migrations/20260412000000_privacy_and_ads.sql, lib/services/privacy_audit_service.dart, lib/services/time_capsule_service.dart, lib/services/canvas_service.dart, lib/features/settings/presentation/screens/privacy_heartbeat_screen.dart, lib/features/settings/presentation/screens/settings_screen.dart, test/features/settings/privacy_heartbeat_test.dart]
autonomous: true
requirements: [PRIVACY-01]
must_haves:
  truths:
    - "Journals, Time Capsules, and Canvas items use E2EE encryption."
    - "Users can see a 'Privacy Heartbeat' log in their settings showing how their data was accessed."
  artifacts:
    - path: "lib/services/privacy_audit_service.dart"
      provides: "Service-level logging for data access"
    - path: "lib/features/settings/presentation/screens/privacy_heartbeat_screen.dart"
      provides: "UI to display access logs"
  key_links:
    - from: "lib/features/settings/presentation/screens/settings_screen.dart"
      to: "lib/features/settings/presentation/screens/privacy_heartbeat_screen.dart"
      via: "Navigator push"
---

<objective>
Audit and implement E2EE encryption for personal content (Journals, Capsules, Canvas) and implement a service-level 'Privacy Heartbeat' system for transparency.

Purpose: To ensure user data is uncompromised through E2EE and to provide users with a clear audit trail of their data access without relying on database triggers.
Output: Encrypted payloads, service-level logging, and a new Privacy Heartbeat UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-monetization-and-privacy-infrastructure/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and Implement E2EE Encryption</name>
  <files>lib/services/time_capsule_service.dart, lib/services/canvas_service.dart</files>
  <action>
    - Audit `TimeCapsuleService`, `CanvasService`, and Journal components for plaintext content storage.
    - Implement E2EE encryption/decryption using the existing `libsignal_protocol_dart` or `EncryptionService` patterns.
    - Ensure that the `content` fields (and other sensitive text) are encrypted before being sent to Supabase and decrypted upon retrieval.
    - Per D-02.
  </action>
  <verify>
    <automated>flutter test test/services/encryption_audit_test.dart || echo "MISSING — Wave 0 must create test/services/encryption_audit_test.dart first"</automated>
  </verify>
  <done>Time Capsules, Canvas items, and Journals are stored securely using E2EE.</done>
</task>

<task type="auto">
  <name>Task 2: Service-Level Privacy Logging & RLS</name>
  <files>supabase/migrations/20260412000000_privacy_and_ads.sql, lib/services/privacy_audit_service.dart, lib/services/time_capsule_service.dart, lib/services/canvas_service.dart</files>
  <action>
    - Create `privacy_audit_logs` table with columns: `id`, `user_id`, `resource_type`, `action`, `timestamp`.
    - Enable RLS for `time_capsules`, `canvas_items`, and `privacy_audit_logs` tables to restrict access to the owner (`user_id` or `owner_id` must match `auth.uid()`).
    - Create `PrivacyAuditService.dart` to log 'READ' and 'WRITE' actions directly to the `privacy_audit_logs` table.
    - Update `TimeCapsuleService` and `CanvasService` fetch/update methods to call `PrivacyAuditService.logAccess(...)` instead of relying on Postgres triggers (since triggers don't support SELECT).
  </action>
  <verify>
    <automated>flutter test test/services/privacy_audit_service_test.dart || echo "MISSING — Wave 0 must create test/services/privacy_audit_service_test.dart first"</automated>
  </verify>
  <done>Service-level logging correctly inserts audit records into Supabase, and strict RLS is applied.</done>
</task>

<task type="auto">
  <name>Task 3: Privacy Heartbeat UI & Tests</name>
  <files>lib/features/settings/presentation/screens/privacy_heartbeat_screen.dart, lib/features/settings/presentation/screens/settings_screen.dart, test/features/settings/privacy_heartbeat_test.dart</files>
  <action>
    - Create a screen to fetch and display the last 50 entries from `privacy_audit_logs` for the current user.
    - Add a navigation tile to `SettingsScreen` under 'Privacy' labeled 'Privacy Heartbeat'.
    - Create `test/features/settings/privacy_heartbeat_test.dart` to verify the UI renders the logs correctly.
  </action>
  <verify>
    <automated>flutter test test/features/settings/privacy_heartbeat_test.dart</automated>
  </verify>
  <done>Privacy Heartbeat screen correctly displays audit logs, is accessible from settings, and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Supabase | Untrusted input crosses here to access RLS-protected tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Information Disclosure | `time_capsules` | mitigate | Strict RLS policies ensuring only owners can see their capsules, plus E2EE |
| T-08-01-02 | Tampering | `privacy_audit_logs` | mitigate | RLS policies ensuring users can only read their own logs and cannot modify them |
</threat_model>

<verification>
- Run `flutter test test/features/settings/privacy_heartbeat_test.dart` and ensure tests pass.
- Verify encrypted payloads in the Supabase dashboard.
</verification>

<success_criteria>
- Personal data is verified to be secure via E2EE and restricted via RLS.
- Users have a functional Privacy Heartbeat audit log powered by service-level events.
</success_criteria>

<output>
After completion, create `.planning/phases/08-monetization-and-privacy-infrastructure/08-01-SUMMARY.md`
</output>