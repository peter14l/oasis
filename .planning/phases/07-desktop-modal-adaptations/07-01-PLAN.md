---
phase: 07-desktop-modal-adaptations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/features/messages/presentation/widgets/modals/message_options_menu.dart]
autonomous: true
requirements: [DESKTOP-01, DESKTOP-02]
must_haves:
  truths:
    - "MessageOptionsMenu shows reactions picker on desktop (right-click)"
    - "Desktop reactions are identical to mobile reactions picker"
    - "Desktop message menu appears at cursor position, not blocking the message"
  artifacts:
    - path: "lib/features/messages/presentation/widgets/modals/message_options_menu.dart"
      provides: "Desktop message context menu with reactions"
      min_lines: 200
    - path: "lib/widgets/messages/message_reactions.dart"
      provides: "Reactions picker component (integrated into menu)"
---

<objective>
Extend the existing MessageOptionsMenu (desktop context menu) to include reactions picker functionality that currently exists in MessageOptionsSheet.

Purpose: Desktop users get the same full functionality as mobile users when right-clicking messages.
Output: Updated MessageOptionsMenu with reactions integration.
</objective>

<execution_context>
@D:/oasis/.opencode/get-shit-done/workflows/execute-plan.md

<read_first>
- lib/features/messages/presentation/widgets/modals/message_options_menu.dart (existing desktop menu)
- lib/features/messages/presentation/widgets/modals/message_options_sheet.dart (mobile sheet with reactions)
- lib/widgets/messages/message_reactions.dart (reactions picker component)
- lib/features/messages/presentation/screens/chat_screen.dart (usage of the menu, lines 421-476)
</read_first>

<action>
## Task: Add Reactions Picker to MessageOptionsMenu

**Files to modify:**
- `lib/features/messages/presentation/widgets/modals/message_options_menu.dart`

**Implementation:**

1. Add imports:
   ```dart
   import 'package:oasis/widgets/messages/message_reactions.dart';
   ```

2. Add `onReactionSelected` callback parameter to the constructor:
   ```dart
   final Function(String) onReactionSelected;
   ```

3. Modify message options to show reactions picker in a submenu OR as a second layer:
   - Add "Add Reaction" menu item that opens a mini reactions picker
   - Use the existing `MessageReactionPicker` component from message_reactions.dart
   - Position the picker near the selected message

4. Alternative approach (simpler): Show reactions as a scrollable horizontal list of emoji buttons in the context menu itself, or open a secondary popup.

**Why this matters:** Currently desktop users right-click and get a limited menu (reply, forward, copy, unsend). Mobile users long-press and get the full sheet with reactions picker. Desktop users should have the same reactions capability.

**Implementation Details:**
- Use `MessageReactionPicker.onReactionSelected` callback pattern
- Pass through to the same callback chain as MessageOptionsSheet uses
- Keep existing menu structure, add "Add Reaction" as new menu item before the divider
</action>

<verification>
<automated>
# Must verify:
# 1. File compiles without errors
grep -l "onReactionSelected" "lib/features/messages/presentation/widgets/modals/message_options_menu.dart"
# 2. File has minimum lines to include reactions
wc -l "lib/features/messages/presentation/widgets/modals/message_options_menu.dart" | awk '{print $1}'
# 3. Has import for message_reactions
grep "message_reactions.dart" "lib/features/messages/presentation/widgets/modals/message_options_menu.dart"
</automated>

<done>
MessageOptionsMenu now supports reactions when right-clicking messages on desktop. Users can add/reactions from the desktop context menu just like mobile users long-press. The reactions picker appears properly positioned near the message.
</done>