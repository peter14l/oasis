---
phase: 07-desktop-modal-adaptations
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified: []
autonomous: true
requirements: [DESKTOP-01, DESKTOP-02]
must_haves:
  truths:
    - "At least 2 additional modal sheets have desktop context menu alternatives"
    - "Desktop right-click triggers work on widgets that open modals"
  artifacts:
    - path: "lib/widgets/share_sheet.dart"
      provides: "Desktop context menu or sidebar for share options (IF NEEDED)"
    - path: "lib/widgets/account_switcher_sheet.dart"
      provides: "Desktop context menu for account switcher (IF NEEDED)"
---

<objective>
Assess and adapt additional high-priority modal sheets for desktop.

Purpose: Not all modals need desktop versions - assess which ones are frequently used on desktop and adapt those.
Output: Updated additional modal sheets with desktop alternatives (if needed).
</objective>

<execution_context>
@D:/oasis/.opencode/get-shit-done/workflows/execute-plan.md

<read_first>
- lib/widgets/share_sheet.dart (share options)
- lib/features/auth/presentation/widgets/account_switcher_sheet.dart (account switcher)
- lib/widgets/messages/message_reactions.dart (if reactions sheet is used elsewhere)
</read_first>

<action>
## Task: Assess and Adapt Additional Modals

**Assessment Phase (first):**

1. Analyze which modal sheets are triggered from desktop-accessible UI:
   - Does `share_sheet.dart` get invoked on desktop views?
   - Does `account_switcher_sheet.dart` get invoked on desktop views?
   - Are there other modals from the 44 showModalBottomSheet calls that are desktop-relevant?

2. Determine which ones need desktop adaptations:
   - Only adapt if the modal is reachable from desktop UI paths
   - Prioritize by frequency of use on desktop

**Implementation (if needed):**

For share sheet:
- Create `ShareMenu` class following `MessageOptionsMenu` pattern  
- Wire desktop detection into calling code

For account switcher:
- Create `AccountSwitcherMenu` class
- This may be less critical since desktop typically has different account UIs

**Note:** If assessment shows no additional modals need desktop adaptations, mark this plan as complete with "No additional modals needed" in the done criteria.

This is an assessment-first plan - it may produce minimal code if no additional modals are needed.
</action>

<verification>
<automated>
# Must verify:
# 1. If additional menus created, verify they exist
# 2. If NO additional menus needed, verify assessment was done
# grep or ls commands to check output
</automated>

<done>
Additional modal sheets assessed. Based on assessment:
- (A) If additional desktop menus created: they work correctly on right-click
- (B) If no additional menus needed: documented which modals are desktop-appropriate (mobile-only views)
This plan completes the desktop modal adaptation work per user request.
</done>