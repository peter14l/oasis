---
phase: 07-desktop-modal-adaptations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [lib/features/messages/presentation/widgets/modals/attachment_options_sheet.dart, lib/features/messages/presentation/screens/chat_screen.dart]
autonomous: true
requirements: [DESKTOP-01, DESKTOP-02]
must_haves:
  truths:
    - "AttachmentOptionsMenu exists for desktop right-click on attachment button"
    - "Desktop shows context menu at cursor position"
    - "Mobile shows bottom sheet (unchanged)"
  artifacts:
    - path: "lib/features/messages/presentation/widgets/modals/attachment_options_menu.dart"
      provides: "Desktop context menu for attachment options"
      min_lines: 80
---

<objective>
Create AttachmentOptionsMenu (desktop context menu) and wire it into chat_screen.

Purpose: When desktop users right-click the attachment button, they get a context menu instead of a bottom sheet.
Output: New AttachmentOptionsMenu widget + updated chat_screen to use it.
</objective>

<execution_context>
@D:/oasis/.opencode/get-shit-done/workflows/execute-plan.md

<read_first>
- lib/features/messages/presentation/widgets/modals/attachment_options_sheet.dart (mobile sheet structure)
- lib/features/messages/presentation/widgets/modals/message_options_menu.dart (pattern for desktop menu)
- lib/features/messages/presentation/screens/chat_screen.dart (usage, lines ~483-497)
</read_first>

<action>
## Task: Create AttachmentOptionsMenu for Desktop

**Files to create:**
- `lib/features/messages/presentation/widgets/modals/attachment_options_menu.dart`

**Files to modify:**
- `lib/features/messages/presentation/screens/chat_screen.dart`

**Implementation:**

1. Create `AttachmentOptionsMenu` widget following the `MessageOptionsMenu` pattern:
   ```dart
   class AttachmentOptionsMenu extends StatelessWidget {
     final VoidCallback onPhotoSelected;
     final VoidCallback onVideoSelected;
     final VoidCallback onFileSelected;
     final VoidCallback onAudioSelected;
     final Offset position;
     
     // Uses showMenu() with PopupMenuEntry items
   }
   ```

2. Menu items map to the 4 attachment types:
   - Photo
   - Video  
   - File
   - Audio

3. Wire into chat_screen.dart's `_showAttachmentOptions()` method:
   - Add desktop detection: `MediaQuery.of(context).size.width >= 1000`
   - Desktop: use `AttachmentOptionsMenu(position: ...)`
   - Mobile: keep using `AttachmentOptionsSheet`

**Positioning:**
- For attachment button context menu, position relative to the attachment icon/button
- Use `RelativeRect.fromLTRB()` pattern from existing code

**Verification:**
- Verify desktop (width >= 1000) shows menu at position
- Verify mobile shows bottom sheet (existing behavior)
</action>

<verification>
<automated>
# Must verify:
# 1. New file exists
test -f "lib/features/messages/presentation/widgets/modals/attachment_options_menu.dart" && echo "EXISTS" || echo "MISSING"
# 2. File has minimum content
wc -l "lib/features/messages/presentation/widgets/modals/attachment_options_menu.dart" | awk '{print $1}'
# 3. chat_screen.dart has desktop detection for attachment
grep -n "AttachmentOptions" "lib/features/messages/presentation/screens/chat_screen.dart" | head -5
</automated>

<done>
Desktop users can right-click the attachment button to see a context menu with attachment options. Mobile users continue to see the bottom sheet. The implementation follows the existing desktop menu pattern.
</done>