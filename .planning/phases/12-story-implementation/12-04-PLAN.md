---
phase: 12-story-implementation
plan: 04
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - lib/features/stories/presentation/screens/create_story_screen.dart
  - lib/widgets/stories/music_picker_sheet.dart
autonomous: true

requirements:
  - STORY-05

must_haves:
  truths:
    - "M3E icons used when isM3EEnabled toggle is on"
    - "Adaptive layout for varying screen sizes"
    - "M3E typography and spacing throughout"
  artifacts:
    - path: "lib/features/stories/presentation/screens/create_story_screen.dart"
      provides: "M3E styled Create Story screen"
  key_links:
    - from: "ThemeProvider"
      to: "CreateStoryScreen"
      via: "isM3EEnabled"
---

<objective>
**Plan 4:** M3E Icons Integration + Adaptive Layout

Purpose: Material 3 Expressive icons when M3E toggle enabled, adaptive layout for all screen sizes.
Output: M3E-styled Create Story Screen with adaptive layout.
</objective>

<read_first>
- lib/features/stories/presentation/screens/create_story_screen.dart
- lib/themes/app_theme.dart (for M3E reference if exists)
- Any ThemeProvider usage
</read_first>

<context>
From CONTEXT.md:
- D-10: Use M3E icons when isM3EEnabled = true
- D-11: Support varying screen sizes

Current M3E usage in codebase (from file analysis):
- isM3E toggle via ThemeProvider
- M3E uses: 28dp border radius, 16dp small radius
- M3E typography: w900 bold, -0.5 letter spacing
- M3E icons: rounded variants (_rounded suffix)
</context>

<tasks>

<task type="auto">
<parameter name>Task 1: Apply M3E icons throughout Create Story Screen</name>
<files>lib/features/stories/presentation/screens/create_story_screen.dart</files>
  <action>
Check each icon in the file and apply rounded variants when isM3EEnabled:

Icons to check and replace:
- Icons.close → Icons.close_rounded
- Icons.text_fields → Icons.text_fields_rounded
- Icons.sticky_note → Icons.sticky_note_2_rounded
- Icons.gesture → Icons.gesture_rounded
- Icons.auto_awesome → Icons.auto_awesome_rounded
- Icons.download → Icons.download_rounded
- Icons.send → Icons.send_rounded (or arrow_forward_rounded)
- Icons.download_rounded - already good if present
- Icons.star → Icons.star_rounded / Icons.stars_rounded (for Close Friends)
- Icons.music_note → Icons.music_note_rounded
- Icons.face → Icons.face_retouching_natural_rounded

Apply conditionally:
```dart
IconData icon = isM3E ? Icons.close_rounded : Icons.close;
```
  </action>
  <verify>
    <automated>
# Verify rounded icon usage with M3E check
grep -n "_rounded\|isM3E" lib/features/stories/presentation/screens/create_story_screen.dart | head -20
    </automated>
  </verify>
  <done>M3E icons appear when isM3EEnabled is true</done>
</task>

<task type="auto">
<parameter name>Task 2: Apply M3E typography and spacing</name>
<files>lib/features/stories/presentation/screens/create_story_screen.dart</files>
  <action>
When isM3EEnabled:
- Font weight: use FontWeight.w900 instead of w800
- Letter spacing: use -0.5 instead of 0 (or positive)
- Border radius: 16dp instead of 12dp for containers
- Icon sizes: Larger (28-32 instead of 24)
- Padding: More generous (16dp standard vs 12dp)

Current state has some M3E support:
- Text style uses isM3E ? FontWeight.w900 : FontWeight.bold
- Letter spacing uses isM3E ? -0.5 : 0

Verify ALL text styles have this pattern. Apply M3E to:
- Tool labels
- Button text
- Input text
- Filter names
  </action>
  <verify>
    <automated>
# Verify M3E typography patterns exist
grep -n "FontWeight\.\|w900\|letterSpacing" lib/features/stories/presentation/screens/create_story_screen.dart | head -15
    </automated>
  </verify>
  <done>Typography changes when toggle changes</done>
</task>

<task type="auto">
<name>Task 3: Adaptive layout for varying screen sizes</name>
<files>lib/features/stories/presentation/screens/create_story_screen.dart</files>
<action>
The Create Story Screen should work on:
- Phones (portrait)
- Tablets (portrait + landscape)
- Desktop/large screens

Current layout is stack-based with fixed positions:

1. Use LayoutBuilder to get constraints:
```dart
 LayoutBuilder(
   builder: (context, constraints) {
     final isTablet = constraints.maxWidth > 600;
     final isDesktop = constraints.maxWidth > 900;
   }
 )
```

2. Adjust layout based on screen size:
- Side toolbar: Position changes on tablets
- Bottom toolbar: Full width on mobile, centered on tablet+
- Text editor: Full screen on mobile, dialog/modal on tablet

3. Adjust sizes:
- Icon sizes: 28 (phone), 32 (tablet), 36 (desktop)
- Button sizes: 48 (phone), 56 (tablet+)
- Side toolbar position: left edge (phone), left: 32 (tablet)
  </action>
  <verify>
    <automated>
# Verify adaptive layout exists
grep -n "LayoutBuilder\|maxWidth\|constraints" lib/features/stories/presentation/screens/create_story_screen.dart | head -10
    </automated>
  </verify>
  <done>Layout adapts to screen size changes</done>
</task>

</tasks>

<verification>
- [ ] M3E icons appear when isM3EEnabled = true in Settings
- [ ] Typography changes with toggle
- [ ] Layout works on phone and tablet sizes
</verification>

<success_criteria>
M3E icons integrated, adaptive layout working
</success_criteria>

<output>
After this plan: .planning/phases/12-story-implementation/12-04-SUMMARY.md
</output>