---
phase: 12-story-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/stories/presentation/screens/create_story_screen.dart
autonomous: true

requirements:
  - STORY-01
  - STORY-02

must_haves:
  truths:
    - "User can add multiple text overlays to story"
    - "User can delete text by dragging to trash"
    - "User can change text background mode (none/solid/dimmed)"
    - "User can select different fonts for text"
  artifacts:
    - path: "lib/features/stories/presentation/screens/create_story_screen.dart"
      provides: "Text overlay system with multi-text support"
  key_links:
    - from: "create_story_screen.dart"
      to: "StoryText model"
      via: "TextField + GestureDetector"
---

<objective>
**Plan 1:** Enhance Text Overlay System - Add multiple text support, background modes, and font styling options.

Purpose: Full Instagram-style text overlays for stories.
Output: Enhanced CreateStoryScreen with robust text editing.
</objective>

<read_first>
- lib/features/stories/presentation/screens/create_story_screen.dart (lines 1-270, 444-510)
- lib/features/stories/domain/models/story_entity.dart (StoryText class)
</read_first>

<context>
From CONTEXT.md:
- D-02: Users can ADD multiple text overlays via "Aa" button
- D-03: Support 3 background modes: None (transparent), Solid color, Dimmed (black54)
- D-04: Font options: Multiple font styles, Color picker, Alignment options

From UI Brand:
- M3E icons use rounded variants
- M3E spacing: 16dp for containers, 12dp for buttons
- M3E typography: w900 for bold, letter-spacing -0.5
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable multi-text add/delete functionality</name>
  <files>lib/features/stories/presentation/screens/create_story_screen.dart</files>
  <action>
1. Remove the initState that pops with "Feature undergoing polish" message
2. The text list already exists (_texts List<StoryText>)
3. AddNewText() already clears controller and shows _isCaptionVisible
4. The trash area _buildTrashArea() already exists with _isTextOverTrash tracking
5. Verify the drag-to-delete flow works: onPanEnd checks distance to trash position and removes text
6. The issue: ensure each text in _texts has unique position when added
  </action>
  <verify>
    <automated>
# Verify code compiles and has multi-text support
grep -n "_texts\." lib/features/stories/presentation/screens/create_story_screen.dart | head -20
    </automated>
  </verify>
  <done>Multiple texts can be added via "Aa" button, edited by tapping, deleted by dragging to trash area</done>
</task>

<task type="auto">
  <name>Task 2: Implement text background modes</name>
  <files>lib/features/stories/presentation/screens/create_story_screen.dart</files>
  <action>
The _textBackgroundMode already cycles through 0,1,2:
- 0 = None (transparent)
- 1 = Solid color (t.color.withAlpha(230))
- 2 = Dimmed (Colors.black54)

The background rendering in _texts builder shows these modes. Verify it works:
- Background mode cycling via text button in _buildTextEditor()
- Visual rendering applies correct background based on mode
  </action>
  <verify>
    <automated>
# Verify background mode logic exists
grep -n "backgroundMode" lib/features/stories/presentation/screens/create_story_screen.dart | head -10
    </automated>
  </verify>
  <done>Cycling through background modes shows: transparent → solid colored → dimmed black</done>
</task>

<task type="auto">
  <name>Task 3: Add font style selector</name>
  <files>lib/features/stories/presentation/screens/create_story_screen.dart</files>
  <action>
1. Add more font style options to the text editor:
   - Current: _selectedFontIndex (single font)
   - Add: List of font styles like:
     - 'Classic' (bold, normal)
     - 'Modern' (neue, bold) 
     - 'Typewriter' (courier, bold)
     - 'Neon' (glow effect)
     - 'Strong' (black, wider)
   
2. Add font style picker UI in _buildTextEditor() below color picker
3. Apply selected font via TextStyle with conditional fontFamily
  </action>
  <verify>
    <automated>
# Verify font options exist
grep -n "fontIndex\|fontStyle\|fontFamily" lib/features/stories/presentation/screens/create_story_screen.dart | head -15
    </automated>
  </verify>
  <done>Font style selector shows 5+ options, selected font applies to text</done>
</task>

</tasks>

<verification>
- [ ] Compile passes: flutter analyze lib/features/stories
- [ ] Multi-text can be added (tap "Aa" multiple times)
- [ ] Edit text by tapping existing text
- [ ] Drag text to trash area deletes it
- [ ] Background mode cycles correctly
- [ ] Font selector appears in text editor
</verification>

<success_criteria>
All text overlay operations work: add, edit, delete, background mode, font selection
</success_criteria>

<output>
After this plan: .planning/phases/12-story-implementation/12-01-SUMMARY.md
</output>