---
phase: 12-story-implementation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/widgets/stories/music_picker_sheet.dart
  - lib/services/spotify_service.dart
autonomous: true

requirements:
  - STORY-03

must_haves:
  truths:
    - "Music search returns results from Spotify API or filtered fallback"
    - "Initial state shows featured tracks (not empty)"
    - "Draggable list to view more songs"
    - "Artwork picker shows multiple options for songs"
    - "Search actually works and returns matches"
  artifacts:
    - path: "lib/widgets/stories/music_picker_sheet.dart"
      provides: "Music picker UI with search and featured tracks"
    - path: "lib/services/spotify_service.dart"
      provides: "Spotify search and featured tracks"
  key_links:
    - from: "music_picker_sheet.dart"
      to: "spotify_service.dart"
      via: "loadFeatured() + search(query)"
---

<objective>
**Plan 2:** Fix Music Picker - Search functionality, initial featured tracks, visual redesign, artwork picker, draggable list.

Purpose: Instagram-style music picker with Spotify integration that actually works.
Output: Fully functional music picker that shows results when searching.
</objective>

<read_first>
- lib/widgets/stories/music_picker_sheet.dart (entire file)
- lib/services/spotify_service.dart (entire file)
</read_first>

<context>
From CONTEXT.md:
- D-07: Music Picker visual overhaul, search MUST work, initial state with featured tracks
- D-08: Music sticker must be draggable to any position

Issues identified:
1. Search returns results but falls back to featuredTracks when no API key - WORKS but may not return matches
2. getFeaturedTracks() returns static list - SHOULD show on initial load
3. Music sticker rendered but NOT draggable - need position tracking

From UI Brand:
- M3E: 28dp border radius top, 16dp container radius
- M3E: Extra Large elevation and spacing
</context>

<tasks>

<task type="auto">
<parameter name>Task 1: Fix search and verify featured tracks load</name>
<files>lib/widgets/stories/music_picker_sheet.dart</files>
<action>
Current state analysis:
- _loadFeatured() calls spotifyService.getFeaturedTracks() - returns static list ✓
- _search(String query) calls spotifyService.searchTracks(query)
  - If no API key: filters featuredTracks by title/artist contains query
  - Returns filtered list if matches found, else returns all featured

Issue: "Search doesn't bring up results" - likely means search IS working but filtering too strict or returns when query doesn't match

Fix:
1. Ensure on initial load, featured tracks show (not empty list)
2. In search, if query matches featured tracks by title/artist, return those
3. Add search result count indicator
  </action>
  <verify>
    <automated>
# Verify search and featured tracks flow
grep -n "loadFeatured\|searchTracks\|getFeaturedTracks" lib/widgets/stories/music_picker_sheet.dart
    </automated>
  </verify>
  <done>Initial load shows 5 featured tracks, search "weeknd" returns matching tracks</done>
</task>

<task type="auto">
<parameter name>Task 2: Visual overhaul of music picker</name>
<files>lib/widgets/stories/music_picker_sheet.dart</files>
<action>
Current UI has basic ListView. Overhaul for Instagram-style:
1. Add draggable list with more songs (horizontal scroll + load more)
2. Add larger artwork display
3. Better visual hierarchy with album art prominent
4. Use M3E styling throughout

Specific changes:
- Container: 85% height, 28dp border radius (M3E)
- ListTiles: More padding, larger album art (56dp)
- Add section headers: "Featured", "Results for X"
- Add "Load more" button at bottom of list
  </action>
  <verify>
    <automated>
# Verify M3E styling present
grep -n "isM3E\|M3E\|28\|16" lib/widgets/stories/music_picker_sheet.dart | head -10
    </automated>
  </verify>
  <done>Music picker shows featured tracks with prominent album art, scrollable list</done>
</task>

<task type="auto">
<parameter name>Task 3: Add artwork picker for selected song</name>
<files>lib/widgets/stories/music_picker_sheet.dart</files>
<action>
User should be able to select from various artwork forms after choosing a song:
1. When user taps a track (not just preview button), show artwork picker first
   OR add "Artwork" button after selection showing different artwork sizes
2. Show 3-4 artwork style options:
   - Original square
   - Blurred background (wide)
   - Circle crop
   - Full bleed
3. Let user click their preferred artwork style
4. Pass selected artwork style to StoryMusicEntity
  </action>
  <verify>
    <automated>
# Verify artwork selection mechanism exists
grep -n "artwork\|Artwork" lib/widgets/stories/music_picker_sheet.dart | head -10
    </automated>
  </verify>
  <done>After selecting song, user can choose artwork display style</done>
</task>

</tasks>

<verification>
- [ ] Initial load shows featured tracks (not empty)
- [ ] Search "weeknd" returns Blinding Lights
- [ ] Music picker has visual overhaul with M3E styling
- [ ] Artwork picker shows options after tapping track
</verification>

<success_criteria>
Music search works, shows results, has artwork picker option
</success_criteria>

<output>
After this plan: .planning/phases/12-story-implementation/12-02-SUMMARY.md
</output>