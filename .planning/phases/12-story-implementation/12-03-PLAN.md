---
phase: 12-story-implementation
plan: 03
type: execute
wave: 2
depends_on: [12-02]
files_modified:
  - lib/features/stories/presentation/screens/create_story_screen.dart
  - lib/features/stories/presentation/providers/stories_provider.dart
autonomous: true

requirements:
  - STORY-04

must_haves:
  truths:
    - "Music sticker is draggable to any position on screen"
    - "After posting story, feed stories bar updates instantly"
    - "Position saved in story metadata"
  artifacts:
    - path: "lib/features/stories/presentation/screens/create_story_screen.dart"
      provides: "Draggable music sticker"
    - path: "lib/features/stories/presentation/providers/stories_provider.dart"
      provides: "Instant state refresh"
  key_links:
    - from: "stories_provider.dart"
      to: "StoriesBar widget in feed"
      via: "notifyListeners() + loadMyStories()"
---

<objective>
**Plan 3:** Draggable Music Sticker + Instant Stories Bar Refresh

Purpose: Music sticker can be positioned anywhere on story, posts immediately reflect in feed.
Output: Draggable music sticker, instant stories bar update.
</objective>

<read_first>
- lib/features/stories/presentation/screens/create_story_screen.dart (lines 610-650 - music sticker widget)
- lib/features/stories/presentation/providers/stories_provider.dart
- lib/features/stories/domain/models/story_entity.dart (StoryMusicEntity)
- lib/features/feed/presentation/widgets/stories_bar.dart
</read_first>

<context>
From CONTEXT.md:
- D-08: Music sticker rendered on story canvas but NOT draggable - must add position tracking
- D-09: After posting, stories appear INSTANTLY - no reload required

Currently:
- Music sticker shown with _buildMusicStickerWidget()
- Position is fixed - no GestureDetector for dragging
- Provider calls loadMyStories() after createStory() - should trigger UI
</context>

<tasks>

<task type="auto">
<parameter name>Task 1: Make music sticker draggable</name>
<files>lib/features/stories/presentation/screens/create_story_screen.dart</files>
  <action>
Current: Music sticker rendered at Positioned but with no gesture handling.

Make it draggable (similar to text draggable):
1. Add _musicPosition (Offset) state variable
2. Wrap the music sticker widget with GestureDetector
3. Add onPanStart, onPanUpdate, onPanEnd handlers
4. Update position during drag, save on release
5. Track position similar to how text position is tracked
6. Include position in story metadata: 'music_position': {x, y}
  </action>
  <verify>
    <automated>
# Verify draggable music implementation
grep -n "_musicPosition\|onPan.*music\|music.*Gesture" lib/features/stories/presentation/screens/create_story_screen.dart | head -5
    </automated>
  </verify>
  <music_sticker_draggable>
    <pattern>
    Music sticker can be dragged to any position on the story canvas.
    Position is saved and persists on story creation.
    </pattern>
  </verify>
</task>

<task type="auto">
<parameter name>Task 2: Verify instant stories bar refresh</name>
  <files>lib/features/stories/presentation/screens/create_story_screen.dart</files>
  <action>
After _createStory() completes successfully:
1. current code calls: context.read<StoriesProvider>().loadMyStories()
2. Then context.pop(true) - returns to previous screen
3. StoriesProvider.loadMyStories() updates _state.userStories and calls notifyListeners()

The flow is correct - when provider updates and notifies, any widget listening should rebuild.

Verify StoriesBar in feed_screen.dart:
1. Uses Consumer<StoriesProvider> or context.watch
2. Listens to storyGroups / userStories
3. Rebuilds when provider notifies

If not working, fix:
- Ensure StoriesBar is wrapped in Consumer/Provider.of
- Ensure state update triggers notifyListeners()
- Check for any loading states that block update
  </action>
  <verify>
    <automated>
# Verify stories provider usage in feed
grep -n "StoriesProvider\|storyGroups\|userStories" lib/features/feed/presentation/screens/feed_screen.dart | head -10
    </automated>
  </verify>
  <stories_instant_refresh>
    <pattern>
    After posting a story, it appears in the stories bar immediately.
    User does NOT need to navigate back or refresh.
    </pattern>
  </verify>
</task>

</tasks>

<verification>
- [ ] Music sticker position can be changed by dragging
- [ ] Position persists after dragging stops
- [ ] Stories bar shows new story immediately after posting
- [ ] No manually refresh needed
</verification>

<success_criteria>
Music sticker draggable, instant stories bar refresh works
</success_criteria>

<output>
After this plan: .planning/phases/12-story-implementation/12-03-SUMMARY.md
</output>