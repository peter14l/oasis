---
phase: desktop-native-ux
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - lib/features/feed/presentation/widgets/post_card.dart
  - lib/widgets/messages/message_reactions.dart
autonomous: true
requirements:
  - DESKTOP-01
  - DESKTOP-02

must_haves:
  truths:
    - "Post long-press shows context menu on desktop"
    - "Post tap-hold shows bottom sheet on mobile (unchanged)"
    - "Message long-press shows options on desktop"
  artifacts:
    - path: "lib/features/feed/presentation/widgets/post_card.dart"
      provides: "Post actions with context menu on desktop"
    - path: "lib/widgets/messages/message_reactions.dart" 
      provides: "Message options with desktop context menu"
  key_links:
    - from: "PostCard"
      to: "DesktopContextMenu"
      via: "SecondaryTapHandler wrapper"
---

<objective>
Apply DesktopContextMenu to high-impact widgets: PostCard and MessageReactions.
</objective>

<execution_context>
@D:/oasis/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/desktop-native-ux/11-01-SUMMARY.md
@lib/widgets/gestures/desktop_context_menu.dart
@lib/features/feed/presentation/widgets/post_card.dart
@lib/widgets/messages/message_reactions.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PostCard with context menu</name>
  <files>lib/features/feed/presentation/widgets/post_card.dart</files>
  <action>
Update PostCard widget to use DesktopContextMenu:
1. Import DesktopContextMenu
2. Wrap post options with SecondaryTapHandler for desktop
3. Keep existing onLongPress behavior for mobile
4. Add platform-aware menu:
   - Desktop: right-click shows popup menu
   - Mobile: long-press shows existing bottom sheet

Menu items to include:
- Like/Unlike
- Comment
- Share
- Bookmark
- Copy text (if text available)
- Report (if not own post)
  </action>
  <verify>PostCard imports DesktopContextMenu and uses SecondaryTapHandler</verify>
  <done>Post long-press shows context menu on desktop, mobile unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Update MessageReactions with context menu</name>
  <files>lib/widgets/messages/message_reactions.dart</files>
  <action>
Update MessageReactions widget:
1. Import DesktopContextMenu  
2. Add right-click support for message options
3. Keep mobile long-press unchanged
4. Menu items: React, Reply, Forward, Copy, Delete (if own)
  </action>
  <verify>MessageReactions imports DesktopContextMenu</verify>
  <done>Message options accessible via right-click on desktop</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>PostCard + MessageReactions with context menus</what-built>
  <how-to-verify>
    1. Build and run app on desktop
    2. Right-click on post → context menu appears
    3. Right-click on message → options appear
    4. Long-press on mobile → still shows bottom sheet
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Post right-click shows context menu
- [ ] Message right-click shows options
- [ ] Mobile long-press still works
- [ ] All menu actions trigger correctly
</verification>

<success_criteria>
PostCard and MessageReactions have desktop context menu:
- Desktop: right-click triggers context menu
- Mobile: long-press triggers bottom sheet (unchanged)
- All menu items functional
</success_criteria>

<output>
After completion, create `.planning/phases/desktop-native-ux/11-02-SUMMARY.md`
</output>